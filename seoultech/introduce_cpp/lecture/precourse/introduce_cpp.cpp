#include<iostream>
using namespace std;

//struct => new type like int, double
struct CDAccountVI{
  double balance;
  double interestRate;

  int term;
};//<- can declare structure variable in semicolon 
  //like }account1, account2; 

// CDAccontVI account;
// accout.balace

int main(){
  ios::sync_with_stdio(0);
  cin.tie(0);
}